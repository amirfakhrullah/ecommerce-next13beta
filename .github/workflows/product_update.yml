name: Product Quantity Update on CRON Schedule

on:
  schedule:
    # - cron: "0 12 * * 1"
    - cron: "15 0 * * *"
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Product Quantity Update
        run: curl -X POST -d {} secrets.WEBHOOK_ENDPOINT